using UnityEngine;
using UnityEngine.TestTools;
using NUnit.Framework;
using System.Collections;

/*public class Health_System_Test {

    private GameObject character;
    private HealthSystem hSys;
    int healthBeforeCollision;
    int healthAfterCollision;


    [Test]
    public void Health_System_TestSimplePasses() {
        // Use the Assert class to test conditions.
    }

    // A UnityTest behaves like a coroutine in PlayMode
    // and allows you to yield null to skip a frame in EditMode
    [UnityTest]
    public IEnumerator Health_System_TestWithEnumeratorPasses() {
        character = GameObject.Find("/Character");
        hSys = character.GetComponent<HealthSystem>();
        healthBeforeCollision = hSys.health;

        yield return new WaitForSeconds(5);

        healthAfterCollision = hSys.health;

        Assert.AreNotEqual(healthBeforeCollision, healthAfterCollision);
    }
}*/
